# TravellingSanta

Ensure python-dev is available:

    [yum|apt|brew] install python3.7-dev

In your virtual environment:

    pip install -r requirements.txt
    kaggle competitions download -c traveling-santa-2018-prime-paths
    jupyter lab

